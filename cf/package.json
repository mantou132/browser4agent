{
  "name": "browser4agent-market-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typegen": "wrangler types",
    "seed": "node seed-local-kv.mjs"
  },
  "dependencies": {
    "toolset-parser": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  }
}
